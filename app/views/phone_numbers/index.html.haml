- title @parent

- if @phone_numbers.count > 0
  = render "index_core", :phone_numbers => @phone_numbers

= render :partial => 'shared/create_link', :locals => {:parent => @parent, :child_class => PhoneNumber}